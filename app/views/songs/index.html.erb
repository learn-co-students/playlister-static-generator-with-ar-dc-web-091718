<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li><a href="songs/<%= song.to_slug %>.html"><%= song.artist.name %> - <%= song.name %> - <%= song.genre.name %></a></li>
  <% end %>
</ul>




<!-- songs.each do |song|
song.to_slug
song.artist.name
song.genre.name
end -->
